<UserControl x:Class="VisionHmi.Views.AlarmsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="ACTIVE ALARMS" Style="{StaticResource H1}" VerticalAlignment="Center" />
                <Border Background="{StaticResource Input}" CornerRadius="3" Padding="8,3" Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding ActiveCount}" Foreground="{StaticResource Fault}" FontWeight="Bold" FontFamily="Consolas" />
                </Border>
            </StackPanel>
            <Button Content="ACKNOWLEDGE ALL" Command="{Binding AcknowledgeCommand}" Style="{StaticResource CmdBtn}" HorizontalAlignment="Right" />
        </Grid>

        <Border Grid.Row="1" Background="{StaticResource CardAlt}" BorderBrush="{StaticResource Border}"
                BorderThickness="1,1,1,0" CornerRadius="3,3,0,0" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90" /><ColumnDefinition Width="80" />
                    <ColumnDefinition Width="190" /><ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="PRIORITY" Style="{StaticResource Caption}" />
                <TextBlock Grid.Column="1" Text="STATION" Style="{StaticResource Caption}" />
                <TextBlock Grid.Column="2" Text="TAG" Style="{StaticResource Caption}" />
                <TextBlock Grid.Column="3" Text="DESCRIPTION" Style="{StaticResource Caption}" />
            </Grid>
        </Border>

        <Border Grid.Row="2" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="0,0,3,3">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Active}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{StaticResource Border}" BorderThickness="0,0,0,1" Padding="12,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="90" /><ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="190" /><ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center"
                                            Background="{Binding Priority, Converter={StaticResource PriorityToBrush}}" CornerRadius="3" Padding="7,2">
                                        <TextBlock Text="{Binding Priority}" FontSize="10" FontWeight="Bold" Foreground="White" />
                                    </Border>
                                    <TextBlock Grid.Column="1" Text="{Binding Station}" VerticalAlignment="Center" Foreground="{StaticResource TextSecondary}" FontSize="12" />
                                    <TextBlock Grid.Column="2" Text="{Binding Name}" VerticalAlignment="Center" Foreground="{StaticResource TextPrimary}" FontFamily="Consolas" FontSize="12" />
                                    <TextBlock Grid.Column="3" Text="{Binding Description}" VerticalAlignment="Center" Foreground="{StaticResource TextPrimary}" FontSize="12" TextTrimming="CharacterEllipsis" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
